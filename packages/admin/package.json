{
  "name": "@augejs/poppy-admin",
  "version": "0.0.4",
  "description": "",
  "main": "./dist/main.js",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsc --build ./tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alex.Zhang",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "@types/js-yaml": "^3.12.5",
    "@types/speakeasy": "^2.0.5",
    "dotenv": "^8.2.0",
    "dotenv-expand": "^5.1.0",
    "speakeasy": "^2.0.0"
  }
}
